# 🩸 Blood Donor Management System - Project Report & Team Roadmap

## 👤 Part 1: Manager's Work Progress (For Teacher Submission)
**Author: Emon Ahmed Joy**

I have successfully designed and implemented the foundational architecture of the Blood Donor Management System. My focus was on creating a professional-grade user experience and a robust logic framework.

### Key Contributions:
1.  **Professional UI/UX Design**:
    *   Developed a custom `GradientPanel` featuring high-performance, animated blood-cell particles to create a modern and "alive" feel.
    *   Implemented `RoundedButton` with custom gradients, pill-shaped geometry, and interactive hover effects.
    *   Designed a tabbed, professional `LoginPage` and unified the visual identity across all 8+ UI pages.
2.  **Advanced Logic & Features**:
    *   **Proximity-Based Search**: Created a scoring algorithm that prioritizes donors by Blood Group, then City, then State.
    *   **Live Notification System**: Built a centralized "Update Flag" system that alerts users and donors via UI popups when their requests are updated.
    *   **Admin Command Center**: Designed a multi-tabbed administration panel with global search (by email) and role-based moderation (Block/Unblock).
3.  **Security & System Integrity**:
    *   Implemented a login restriction system that prevents blocked users from accessing the application.
    *   Structured the `DataStore` to handle multi-role interactions (User, Donor, Admin) seamlessly.
4.  **Code Quality & Branding**:
    *   Applied global author branding (`@author Emon Ahmed Joy`) to all 13 Java source files.
    *   Ensured the project is 100% stable and compilable.

---

## 👥 Part 2: Team Work Distribution (Instructions for Members)

### 1. Database Member (Durjoy)
**Branch:** `Service-Database`
**Task Level:** Advanced (Core Persistence)
**Goal:** Replace the temporary `DataStore.java` lists with a real MySQL Database.

**Action Steps:**
1.  **Setup MySQL**: Create a database named `blood_management` with tables: `users`, `donors`, and `blood_requests`.
2.  **JDBC Integration**: Add the MySQL Connector JAR to the project.
3.  **DataStore Migration**: 
    *   Update `DataStore.java` to use `Connection`, `PreparedStatement`, and `ResultSet`.
    *   Change the `static` lists to methods that fetch/save data directly to MySQL.
    *   Ensure that when a user registers, their data is saved to the database.

---

### 2. Backend Member 1 (Lisan)
**Branch:** `Service-Backend-Logic-1`
**Task Level:** Easy (Feature Extension)
**Goal:** Improve the Donor Profile and Request details.

**Action Steps:**
1.  **Profile Editing**: Add a "Edit Profile" button on the `DonorProfilePage`.
2.  **Field Update**: Allow donors to update their "Location" or "Availability Status" (Available/Busy).
3.  **Request History**: Add a "History" tab in the Donor dashboard to see all past requests they have already accepted or declined.

---

### 3. Backend Member 2 (Nasir)
**Branch:** `Service-Backend-Logic-2`
**Task Level:** Easy (UI Enhancement & Reporting)
**Goal:** Add visual reports and "About" information.

**Action Steps:**
1.  **System Stats**: On the `AdminPage`, add a small panel that shows totals: "Total Donors", "Pending Requests", and "Total Success Stories".
2.  **App Info**: Create a small `AboutPage.java` that explains the purpose of the app and lists the team members.
3.  **Input Validation**: Add simple checks to the `RegistrationPage` (e.g., ensure the password is at least 6 characters long or the email contains an `@` symbol).

---

## 🛠️ How to Start (For Members)
1.  **Clone** the repository from GitHub.
2.  **Switch** to your assigned branch: `git checkout [branch-name]`.
3.  **Work** on your assigned steps.
4.  **Commit** your changes: `git add . && git commit -m "Done: [description]"`.
5.  **Push** to your branch (Do NOT push to master).

---
*Generated by Emon Ahmed Joy - Project Manager*
